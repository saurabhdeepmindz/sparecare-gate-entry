One-line: the honesty section beneath the annotations, naming what the wireframe does not show.

```jsx
<MissingSection
  statesNotDrawn={['Offline — what remains usable with no signal','Mid-scan — the moment between trigger and decode']}
  detailNotSpecified={['Exact copy on the mismatch dialog','Whether the WIP counters refresh on save']}
  mostImportant="Unit-level counting is not drawn here at all, because FR-006.18 defers it to Consignment Check." />
```

Keep each bullet to one line naming the gap, not explaining it. The most-important omission earns a sentence or two.
