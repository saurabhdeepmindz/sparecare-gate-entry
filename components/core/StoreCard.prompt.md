One-line: a store result on the SpareCare network, used in lists of nearby stores.

```jsx
<StoreCard name="Bansal Auto Spares" location="Sadar Bazaar, Meerut · 2.4 km" matchLabel="9 matching parts" />
```

Use real locality names in examples, as the brand voice requires. The card carries a 1px border and no shadow — cards in this system are drawn with lines, never lifted.
