One-line: the collapsible panels the FRD specifies on Screens 4a, 4b and 6a.

```jsx
<Accordion title="Capture Invoice Details" open={open} onToggle={toggle} badge={<Tag tone="info" dot={false}>3 added</Tag>}>
  …
</Accordion>
```

These are collapsed by default in the source artwork — the screens are long, and on a handheld an operator should see the whole task before opening one section.
