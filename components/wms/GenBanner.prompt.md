One-line: shown at the foot of Screen 1 once the GEN exists; the number is the master reference for everything downstream.

```jsx
<GenBanner gen="GEN-2025-05-20-0012" footnote="Print slip or share reference" />
```

Always mono for the number. Do not restyle the green to teal — it is doing traffic-light duty, and the brand document keeps those three signal colours outside the palette deliberately.
