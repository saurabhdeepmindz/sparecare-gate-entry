One-line: the title bar at the top of every handheld WMS screen.

```jsx
<ScreenHeader title="Create GEN" onBack={goBack} />
```

The title uses Barlow Semi Condensed bold in deep teal — the header is not a filled navy bar. The chevron is a plain ‹ glyph, as in the source artwork.
